description: Not Found
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          enum: [404]
          description: The HTTP Status Code
        message:
          type: string
          description: An error message
