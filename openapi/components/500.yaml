description: Internal Server Error
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          enum: [500]
          description: The HTTP Status Code
        message:
          type: string
          description: An error message
