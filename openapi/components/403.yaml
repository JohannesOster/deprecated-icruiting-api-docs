description: Forbidden
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          enum: [403]
          description: The HTTP Status Code
        message:
          type: string
          description: An error message
